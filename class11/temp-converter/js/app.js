	Fahrenheit = Celsius * 1.8 + 32
	Celsius = (Fahrenheit - 32) / 1.8

//Set variables for interactive elements
var units = document.getElementById('units');
var temp-value = document.getElementById('temp-value');


//Set up variables for chunks of data
var temp-convert;

//Set up trigger
if (units = Farenheit) {
	$('#temp-convert').val((temp-value - 32)/1.8);
}

if (units = Celsius) {
	$('#temp-convert').val((temp-value * 1.8) + 32);
}